spring:
  datasource:
    url: jdbc:h2:mem:vet_api_demo
  jpa:
    properties:
      hibernate:
        jdbc:
          '[time_zone]': America/New_York

logging:
    level:
        root: warn
        '[com.kmatheis.vet]': debug